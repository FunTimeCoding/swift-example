// This is a multi module script, meaning all classes declared in `src` can be used.

print("Hello friend.")

var counter = 0

for argument in Process.arguments {
    print("Argument \(counter) is: \(argument)")
    counter++
}

var fw = Firewood()
fw.burn()

// vim: ft=swift
